specVersion: "v1beta"
serviceId: "popics"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "popics-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
